# PRD — 청첩장 사이트 UI/기능 개선 (2026-09-01)

> 이 문서는 2026-09-01 세션에서 진행된 작업 요청과 구현 내용을 PRD(제품 요구사항 문서) 형식으로 정리한 기록입니다.
> 전체 기능 구조는 [`청첩장_기능분석.md`](청첩장_기능분석.md), 갤러리 상세 구조는 [`갤러리_슬라이더_구조.md`](갤러리_슬라이더_구조.md) 참고.

## 1. 개요

- **대상 페이지**: `index.html` (신랑측 `groom-parent_2.html`, 신부측 `bride-parent_3.html` 변형 포함), `photo.html`(관리자), `setting.json`
- **배경**: 이전 세션에서 진행되던 커버 테마 실험(골드·버건디 톤)이 커밋되지 않은 채로 남아있던 것을 발견 → 겨울 테마 커밋 상태로 롤백 후, 아래 요구사항들을 새로 적용
- **관련 커밋**: `31c7ac1` → `01eca5e` → `3891257` → `26f4b9b` → `690357a` (5건, origin/main 배포 완료)

## 2. 요구사항 및 구현 내역

### R1. 커버 배경 — 산 이미지 교체
| 항목 | 내용 |
|---|---|
| 요구사항 | 커버의 SVG 후지산 실루엣을 사용자가 제공한 `배경.png` 기반의 실사 느낌 나는 웅장한 설산 이미지로 교체 |
| 구현 | `배경.png`에서 텍스트가 없는 산 영역만 크롭·업스케일·상단 알파 페이드 가공 → `images/cover-mountain.png` 생성. `#cover-fuji` 요소를 `<svg>` → `<img>`로 교체, opacity 0.16→0.55, width 150%→170%(max-width 640→720px)로 확대해 존재감 강화 |
| 상태 | ✅ 완료 (배포 확인) |

### R2. 눈 결정(Snow) 효과 축소
| 항목 | 내용 |
|---|---|
| 요구사항 | 기존 눈송이(`.petal`)를 크기 20% 축소, 반투명한 "눈 결정" 느낌으로 변경 |
| 구현 | 인라인 크기 14~18px → 11~14px로 축소. 1차 작업 시 그라데이션 알파값과 keyframe 최대 불투명도를 과도하게 낮춰 눈이 거의 안 보이는 회귀 발생 → 그라데이션은 원래의 불투명 코어 색상으로 복원하고 keyframe 불투명도만 완만하게(0.65/0.85/0.30 → 0.55/0.75/0.25) 낮춰 가시성 회복 |
| 상태 | ✅ 완료 (1차 회귀 발견 → 2차 수정으로 해결) |

### R3. 마음 전하기 — 신부측 아버지 계좌 카드 삭제
| 항목 | 내용 |
|---|---|
| 요구사항 | "마음 전하기(송금)" 섹션에서 신부측 아버지 성함·계좌번호 카드 제거 |
| 구현 | `parent-card-bf` 카드(HTML) 및 `parentMap`의 `brideFather` 매핑(JS) 제거. 혼주 소개 섹션의 신부 아버지 전화 연결 카드는 별개 기능이라 유지 |
| 상태 | ✅ 완료 |

### R4. 갤러리("우리의 이야기") — 화살표 · 확대보기 개선
| 항목 | 내용 |
|---|---|
| 요구사항 | 화살표는 흰 배경 없이 반투명 아이콘만 남기고 사진을 가리지 않게, 사진 자체의 블러/켄번즈 줌인·슬라이드쇼 효과는 제거, 클릭 시 확대되어 스와이프로 넘겨볼 수 있게 |
| 구현 | `.slide-btn`에서 배경·테두리·그림자 제거, 반투명 텍스트 화살표로 변경. `.slide`의 켄번즈 줌인(`scale`)과 마스크 크로스페이드를 제거하고 `translateX` 기반 좌우 슬라이드로 전환. 신규 라이트박스(`lightbox-backdrop`/`lightboxStage` 등) 추가 — 사진 클릭 시 확대, 스와이프로 이전/다음 이동 |
| 후속 수정 | 라이트박스가 탭 시 의도치 않게 닫히는 문제 → 1차: 닫기(X) 버튼으로만 닫히게 변경 → X 버튼이 슬라이드 레이어에 가려 클릭이 안 먹는 z-index 버그 발견 → 최종: 스와이프가 아닌 짧은 탭이면 닫히도록 하고, X 버튼 z-index도 상향 조정해 이중으로 닫을 수 있게 정리 |
| 상태 | ✅ 완료 |

### R5. RSVP(참석여부) 제거 → 방명록(축하 메시지) 기능 신규
| 항목 | 내용 |
|---|---|
| 요구사항 | 참석여부 폼 제거, 그 자리에 기존에 있었던 "댓글" 기능 복원 |
| 조사 결과 | git 전체 이력을 확인한 결과 이 저장소에는 방명록/댓글 기능이 존재한 적이 없음 — 신규 구현으로 처리 |
| 구현 | RSVP 폼(신랑/신부측, 참석여부, 인원수) 및 관련 JS(`submitRsvp`, `renderRsvp`) 제거. 이름+메시지를 남기고 목록으로 보여주는 방명록 UI(`renderGuestbook`, `submitComment`) 신규 추가. 백엔드는 `api/comments.js` 신규 생성(`api/rsvp.js`와 동일한 GET/POST/DELETE + Vercel KV 패턴, 키: `wedding_comments`) |
| 검증 | 배포된 사이트의 `/api/comments`에 대해 POST(저장)→GET(조회)→DELETE(정리)까지 직접 테스트하여 정상 동작 확인 |
| 상태 | ✅ 완료 (배포 및 API 동작 확인) |

### R6. 오시는 길 — 교통정보 카드 개편
| 항목 | 내용 |
|---|---|
| 요구사항 (최종) | Taxi 카드를 Subway로 교체하고, 별도로 있던 Subway 카드(이전에 Car에서 전환한 것)는 삭제. Parking 카드는 가로로 넓혀 아래 줄 전체 너비 1칸으로, 위 줄은 Bus/Subway 2칸으로 배치 |
| 진행 과정 | ① Taxi→Subway 1차 교체 → ② 사용자 요청으로 롤백 후 Car→Subway로 재교체(관리자 페이지에 남아있던 "지하철로 변경" 메모와 일치) → ③ 최종적으로 Taxi 슬롯을 Subway로 흡수하고 중복 Subway 카드 삭제, 레이아웃을 2x1(Bus+Subway / Parking 전체너비)로 확정 |
| 구현 | `index.html`/`groom-parent_2.html`/`bride-parent_3.html`의 `.transport-grid` 마크업과 JS 바인딩(`setTransportInfo`) 수정, `.transport-card-wide { grid-column: 1 / -1; }` CSS 추가. `photo.html` 관리자 폼도 Bus/Subway/Parking 3개 입력으로 정리 (`DEFAULT_TRANSPORT`, `fillTransportFields`, `saveTransport`) |
| 상태 | ✅ 완료 (배포 확인) |

### R7. 신부 영문 이름 표기 수정
| 항목 | 내용 |
|---|---|
| 요구사항 | 신부 영문 이름 표기를 "Bang Hye Yoon" → "Bang Hye Yun"으로 수정 |
| 구현 | `setting.json`의 `wedding.bride.englishName` 값 수정 (커버 영문 이름 표시의 유일한 소스) |
| 상태 | ✅ 완료 (배포 확인) |

## 3. 범위 외 / 후속 과제

- **도메인 리다이렉트 미확인 이슈**: 배포 확인 중 프로덕션 도메인이 `junil-hyeyoon-invite.vercel.app` → `junil-hyeyun-invite.vercel.app`로 리다이렉트되는 것을 발견함. 이름 표기 수정과 맞물려 별도로 도메인/프로젝트명을 변경한 것으로 추정되나, `setting.json`의 `site.url`(카카오톡 공유 미리보기 등에 사용)은 아직 이전 도메인으로 남아있어 갱신이 필요할 수 있음 — **미해결**
- **오시는 길 Subway 안내 문구**: 관리자 페이지(photo.html)에 실제 지하철역·출구·도보시간 정보를 아직 입력하지 않은 상태 (필드만 준비됨) — **입력 필요**
- **RSVP 데이터 정리**: 기존 `/api/rsvp`(및 `result.html`)는 코드상 남아있으나 index.html에서 더 이상 참조하지 않음. 완전히 정리할지 여부는 논의되지 않음 — **결정 필요**

## 4. 영향받은 파일

- `index.html`, `groom-parent_2.html`, `bride-parent_3.html` — 커버 이미지, 눈 효과, 마음전하기, 갤러리/라이트박스, 방명록, 오시는 길 교통정보
- `photo.html` — 오시는 길 교통정보 관리자 폼
- `setting.json` — 신부 영문 이름
- `api/comments.js` — 신규 생성
- `images/cover-mountain.png` — 신규 생성 (커버 배경 산 이미지 에셋)
