# 청첩장 프로젝트 기능 분석

> 모바일 청첩장 웹 사이트의 주요 기능을 정리한 문서입니다.
> 신규 기능 추가·기존 기능 개선·유지보수 시 어떤 영역이 어떤 방식으로 동작하는지 빠르게 파악할 수 있도록 구성했습니다.

최종 갱신: 2026-07-30

---

## 0. 프로젝트 개요

- **유형**: 정적 HTML + Vercel Serverless API (Vercel KV 저장소)
- **설정 체계**: `setting.json`(정적 초기값) + Vercel KV(운영 중 실시간 수정값) 이원화 — 자세한 우선순위는 [2. 설정 관리](#2-설정-관리-settingjson--vercel-kv-이원화) 참고
- **모바일 우선 디자인**: max-width 480px, iOS Safari + Android Chrome 호환
- **컬러**: 겨울 홋카이도 눈꽃축제 톤 — 스틸블루 `#7B95AC` · 아이스블루 `#C5D5E0` · 다크네이비 `#4A5F73` · 페리윙클 `#A3AECF` · 아이스크림 `#F4F7FA` (자세한 배경은 [14. 겨울 테마 리디자인](#14-겨울-테마-리디자인) 참고)

### 페이지 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 메인 청첩장 |
| `groom-parent_2.html` | 신랑측 혼주용 변형 (혼주 메시지/공유 문구 차이) |
| `bride-parent_3.html` | 신부측 혼주용 변형 |
| `link.html` / `girl_link.html` / `man_link.html` | 청첩장 공유 진입 페이지 (메인/신부측/신랑측 분기) |
| `nav.html` | 길찾기 (네이버지도/카카오맵/카카오내비/T맵) — 카카오톡 공유 카드의 "🧭 길찾기" 버튼이 여는 페이지 |
| `cal.html` | 일정 등록 (Google Calendar), 예식 7일 전 알림 이벤트 자동 생성 |
| `photo.html` | **관리자 페이지** — 커버(영상/사진), 갤러리 사진, 예식 일자·일정 사진, 예식장·추가 장소, 오시는 길 교통정보, 계좌·연락처(신랑/신부 표시 이름 포함)를 전부 여기서 관리 (비밀번호 게이트) |
| `result.html` | RSVP 회신 집계 관리 페이지 |

### API 엔드포인트 (`api/*.js`, Vercel Functions + KV)

| 엔드포인트 | 메서드 | 용도 | 저장 키 |
|---|---|---|---|
| `/api/rsvp` | GET/POST/DELETE | 참석 회신 응답 | `rsvp_responses` |
| `/api/photos` | GET/POST/DELETE | 갤러리 사진 (v2 키 분리 + v1 레거시 병합) | `gallery_index` + `photo:<id>` + `gallery_photos` |
| `/api/parents` | GET/PUT | 혼주 연락처 4명 + 신랑·신부 본인 연락처 | `wedding_parent_phones` |
| `/api/accounts` | GET/PUT | 송금 계좌 정보 (신랑/신부 본인 + 양가 부모) | `wedding_accounts` |
| `/api/venue` | GET/PUT | 예식장 정보(이름/홀/길찾기용 이름/주소/좌표/사진/좌표우선여부), 추가 장소 배열, 오시는 길 교통정보 | `wedding_venue` |
| `/api/date` | GET/PUT | 예식 일자(연/월/일/시/분/요일/시간표시문자열) + 예식 일정 섹션 사진 | `wedding_date` |
| `/api/cover` | GET/PUT | 커버 표시 방식(`mode`: video/photo) + 커버 사진 | `wedding_cover` |
| `/api/geocode` | GET | 카카오 로컬 주소 검색 API 프록시 — 주소 문자열 → 위도/경도 변환 (REST 키는 서버에서만 사용) | (KV 저장 없음, 단순 프록시) |

---

## 1. 다중 청첩장 변형 (메인 / 신랑측 / 신부측)

**무엇인가**
같은 결혼식이지만 누가 보내느냐에 따라 (신랑 본인 / 신랑 부모 / 신부 부모) 인사말·서명·공유 문구가 미묘하게 다른 3종 청첩장을 별도 HTML 파일로 운영.

**왜 가치 있는가**
실제 한국 결혼 문화에서 혼주(부모)가 주변에 청첩장을 돌리는 경우가 많고, 본인이 보내는 청첩장과는 어조가 달라야 함 ("저희가 결혼합니다" vs "저희 아들이 결혼합니다"). 한 파일로 분기 처리하면 조건문이 복잡해지고 실수가 늘기 때문에, 차라리 파일을 분리해 변형마다 독립적으로 유지보수.

**구현 난이도와 트레이드오프**
- **장점**: 각 변형이 단순한 정적 HTML, 진입 URL만 다르면 됨 (`link.html` 에서 분기)
- **단점**: **세 파일 drift 위험** — 공통 기능(갤러리 슬라이더, 송금 모달, 길찾기 로직 등)을 한 파일만 수정하고 나머지를 잊으면 페이지마다 동작이 달라짐. 이번 예식장 변경 작업(2026-07)에서도 매번 3개 파일에 동일 패치를 반복 적용해야 했음
- 장기적으로는 공통 JS/CSS 를 외부 파일로 분리하는 리팩터링이 유효 (현재는 HTML 안에 인라인)

**핵심 고려사항**
세 파일 동시 수정 시 반드시 `git diff` 로 동일 패치가 들어갔는지 검토. 카카오 공유 버튼·갤러리 슬라이더·길찾기 로직처럼 자주 손대는 영역은 특히 주의.

---

## 2. 설정 관리 (`setting.json` + Vercel KV 이원화)

**무엇인가**
신랑신부 이름·메시지·계좌 기본값 등은 `setting.json`에, **예식 일자·예식장 정보·추가 장소·오시는 길 교통정보·혼주 연락처·계좌**는 photo.html 관리자 페이지를 통해 **Vercel KV**에 저장한다. 청첩장 페이지는 로드 시 `setting.json`을 먼저 읽고, 이어서 각 KV 엔드포인트(`/api/venue`, `/api/date`, `/api/parents`, `/api/accounts`)를 fetch 해서 **KV 값이 있으면 setting.json 기본값을 덮어쓴다**.

**왜 가치 있는가**
청첩장 내용은 자주 수정된다 (사진 추가, 예식장 변경, 날짜 조정, 혼주 메시지 변경 등). 특히 예식장이 통째로 바뀌는 경우처럼 큰 변경도 **재배포 없이 photo.html에서 바로 반영**할 수 있어야 결혼 준비 과정의 실제 요구를 감당할 수 있다.

**구현 난이도와 트레이드오프**
- **장점**: 데이터-뷰 분리, 재배포 없이 실시간 수정 가능. 비개발자(신랑/신부)가 photo.html UI만으로 예식장 변경·날짜 변경 같은 큰 이벤트도 스스로 처리 가능
- **단점**: 우선순위 규칙(KV > setting.json)을 모르면 "setting.json을 고쳤는데 왜 반영이 안 되지?" 하는 혼란이 생김. 정적 `<meta>` 태그(카카오톡 공유 미리보기용 og:description 등)는 이 KV 동기화 대상이 아니라서 **별도로 손으로 갱신해야 함** (아래 참고)

**핵심 고려사항**
- 권위 순서: **Vercel KV > setting.json**. `setting.json`은 "KV가 비어있을 때의 초기 기본값" 역할만 한다.
- **정적 메타 태그의 한계**: 카카오톡·트위터 공유 시 뜨는 미리보기(`<meta name="description">`, `og:description`, `twitter:description`)는 메신저 봇이 **JS를 실행하지 않고 HTML 원본만 읽어가기 때문에** KV 값이 아무리 바뀌어도 자동 반영되지 않는다. 예식장·날짜가 바뀌면 `index.html`/`groom-parent_2.html`/`bride-parent_3.html`/`setting.json`의 메타 태그 텍스트를 수동으로 갱신해야 한다. (완전 자동화하려면 Vercel Edge/Serverless 함수로 요청마다 KV를 읽어 메타 태그를 실시간 주입하는 SSR 방식 전환이 필요 — 현재는 구조 단순성을 위해 채택하지 않음)
- KV 데이터는 각 카드의 "저장" 버튼을 누를 때 **읽기→병합→쓰기**로 저장된다 (예: 예식장 정보 저장과 교통정보 저장이 같은 `wedding_venue` 키를 공유하므로, 저장 전 기존 값을 GET 해서 병합한 뒤 PUT 해야 서로의 필드를 덮어쓰지 않음).

---

## 3. 사진 갤러리 (업로드 + 슬라이더)

**무엇인가**
`photo.html` 에서 사진을 업로드(base64) 하면 `/api/photos` POST 로 KV 에 저장되고, 청첩장 페이지에서 슬라이더로 자동 재생. 점 인디케이터·하단 썸네일·터치/마우스 스와이프·켄번즈 줌인·그라데이션 페이드 전환 지원.

**왜 가치 있는가**
사진은 청첩장에서 가장 자주 바뀌는 콘텐츠. HTML 을 수정하지 않고 업로드만으로 갱신할 수 있으면 비개발자가 직접 관리 가능. 슬라이더 인터랙션 품질은 청첩장 첫 인상에 큰 영향.

**구현 난이도와 트레이드오프**
- **저장 구조 v2 분리**: 사진별로 `photo:<id>` 키 + `gallery_index` 순서 배열로 저장 → 누적해도 단일 키 크기 폭주 회피 (v1 단일 배열은 KV 1MB 한계에 빠르게 도달)
- **레거시 호환**: GET 시 v2 신규 + v1 레거시(`gallery_photos`) 를 합쳐 반환 → 과거 사진 보존
- **base64 인라인 저장**: 외부 스토리지(S3 등) 불필요하지만 KV 비용↑·전송량↑ → 사진 수가 많아지면 부담. 압축 로직(`compressImage`, maxSize 1080px / quality 0.72)으로 장당 보통 60~200KB 수준 유지

**핵심 고려사항**
- 슬라이더 코드는 3개 HTML 파일에 동일하게 복제됨 (drift 위험)
- 예식장 사진(`venue.photo`)·커버 사진·예식 일정 사진도 모두 동일한 `compressImage` 로직을 재사용해 photo.html에서 업로드 가능 (아래 9번, [13. 커버 표시 관리](#13-커버-표시-관리-영상사진--예식-일정-사진) 참고)
- **업로드 용량 제한**: 원본 파일이 2.5MB를 넘으면 `compressImage()`가 압축 시도 전에 즉시 거부 (갤러리/예식장/커버/예식 일정 사진 4곳 공통). 갤러리는 진행 목록에 "실패: 파일이 너무 큽니다 (X.XMB) — 2.5MB 이하로 올려주세요"로 표시, 나머지 3곳은 동일 메시지를 토스트로 표시
- 슬라이드 전환은 CSS Grid 스택 + `opacity` + `mask-image` 그라데이션으로 부드러운 페이드

---

## 4. 송금 모달 (토스/카카오페이 딥링크 + 폴백 체인)

**무엇인가**
"마음 전하기" 카드 클릭 → 모달에서 토스/카카오페이/계좌번호 복사 중 선택. 앱 딥링크 시도 후 일정 시간 내 앱이 열리지 않으면(`visibilitychange`/`blur` 미감지) 다음 앱으로 폴백, 최종적으로 계좌번호 복사.

**왜 가치 있는가**
한국 결혼식 축의금 문화에서 송금 편의성은 실제 회수율에 직접 영향. 사용자의 폰에 어떤 송금 앱이 설치되어 있는지 알 수 없으므로 자동 폴백이 사용자 친화적.

**구현 난이도와 트레이드오프**
- **장점**: 별도 SDK 없이 URL 스킴 + 이벤트 감지만으로 구현 가능
- **단점**: 딥링크 동작이 OS/브라우저/앱 버전마다 미세하게 다름 → iOS Safari, Android Chrome, In-App Browser(카카오톡 내장 브라우저) 각각 테스트 필요

**핵심 고려사항**
계좌 데이터는 [8. 혼주·계좌·연락처 통합 관리](#8-혼주계좌연락처-통합-관리) 참고. 계좌번호 복사는 `navigator.clipboard.writeText` 우선, 실패 시 `document.execCommand('copy')` 폴백.

---

## 5. 길찾기 (4종 지도 앱 + 좌표/주소 우선순위)

**무엇인가**
오시는 길 카드에서 길찾기 버튼 클릭 → 모달에서 네이버지도/카카오맵/카카오내비/T맵 선택. 이제 **4종 앱 모두 좌표가 있으면 좌표를 우선 사용**하도록 통일됨(이전엔 네이버/카카오맵만 텍스트 검색이었음).

**왜 가치 있는가**
한국 사용자는 네이버지도와 카카오맵 사용자가 비등하고, 운전자는 카카오내비/T맵을 선호. 한 앱만 지원하면 30~50% 사용자가 불편을 겪음. 좌표 기반 이동은 이름이 지도 서비스에 정확히 등록되어 있지 않은 신생 예식장에서도 정확한 위치로 안내한다.

**핵심 개념 3가지**
1. **표시 라벨(title) vs 실제 목적지는 분리된다.**
   - `name` — 청첩장 화면(커버, 오시는 길 사진 라벨)에 표시되는 예식장 이름
   - `navName`(길찾기 검색 이름, 선택) — 길찾기 버튼 라벨과 지도 앱에 표시되는 "제목"으로만 쓰이는 텍스트. 비워두면 `name`을 대신 사용
   - 실제 검색/목적지 지정은 **항상 주소(좌표가 있으면 좌표)**를 사용하고, name/navName은 절대 검색어로 쓰이지 않는다
2. **좌표 우선, 주소는 폴백.** 좌표(`lat`/`lng`)가 있고 `useCoords`가 꺼져있지 않으면 좌표로 정확한 지점을 지정, 없으면 주소로 텍스트 검색.
3. **장소별로 좌표/주소 우선순위를 수동 지정 가능.** photo.html의 "길찾기 시 좌표 우선 사용" 체크박스(예식장 본체·추가 장소 각각)를 해제하면 좌표가 저장돼 있어도 항상 주소로만 검색하도록 강제할 수 있다 (지오코딩 결과가 미묘하게 부정확할 때 대비).

**구현 난이도와 트레이드오프**
- **장점**: Google Maps API 키 불필요 (임베드는 `output=embed`, 길찾기는 외부 앱 위임)
- **단점**: 각 앱별 URL 스킴 규격이 자주 바뀜 → 앱 업데이트마다 동작 검증 필요. 네이버지도는 좌표 기반 "웹" 뷰 URL이 공식적으로 없어서, 좌표가 있을 때도 **모바일 앱 딥링크(`nmap://place?...`)로만** 정확한 지점을 찍고 데스크톱/미설치 시엔 주소 검색으로 폴백

**핵심 고려사항**
- 모바일/데스크톱 분기 없이 "딥링크 시도 → 일정 시간 내 페이지 이탈 없으면 웹 폴백" 패턴(`tryDeepLink` 유사 로직)으로 통일
- 카카오내비/티맵은 `ep=lat,lng` / `goalx,goaly`로 목적지를 정확히 지정하고, `goalname`/표시 이름 자리에는 navName(또는 name)을 라벨로만 사용
- **카카오톡 공유하기(`sendKakao`)의 카드 설명 문구는 `navName`이 아니라 `name`을 사용** — 길찾기용 이름과 공유 카드 문구용 이름이 서로 다른 값을 참조하므로 헷갈리지 않도록 주의
- 다중 장소(추가 장소)의 길찾기도 동일한 모달·로직을 재사용 — [9. 예식장 정보 동적 관리](#9-예식장-정보-동적-관리)의 `extraLocations` 참고

### 5-1. 지도 미리보기 (카카오맵 JS SDK)

**무엇인가**
오시는 길 섹션의 지도 미리보기(임베드)를 좌표가 있을 땐 카카오맵 JS SDK로 실제 지도를 그리고, 좌표가 없거나 SDK 로드에 실패하면 기존 Google Maps 주소검색 임베드(`output=embed`)로 자동 폴백한다. `naver-map-wrap` 안에 `#kakao-map`(JS SDK용 div)과 `#naver-map-iframe`(Google 임베드용, 클래스명은 레거시)을 함께 두고 상황에 따라 하나만 보여준다.

**왜 가치 있는가**
Google 임베드는 API 키 없이 되지만 한국 로드뷰·건물 디테일이 부족해서 실제 지도 앱만큼 정확하지 않다. 카카오맵 JS SDK는 국내 지도 데이터가 훨씬 정확하고 마커·확대 등 인터랙션도 자연스럽다. 다만 SDK는 도메인 화이트리스트가 걸려있어 아무 배포에나 바로 쓸 수 없으므로, 실패해도 화면이 깨지지 않도록 Google 임베드를 안전망으로 유지한다.

**구현 난이도와 트레이드오프**
- **장점**: 좌표가 없는 초기 상태에도 Google 폴백으로 화면이 항상 뜬다.
- **단점**: 카카오맵 JS SDK는 appkey가 등록된 도메인에서만 동작 — 로컬(`localhost`)이나 도메인 미등록 환경에서는 인증 에러가 나거나 지도가 안 뜬다(정상 동작이며 배포 도메인에서 테스트해야 함). `display:none → block` 직후 곧바로 지도를 그리면 컨테이너 크기 계산이 어긋나 중심이 밀리는 카카오맵 알려진 이슈가 있어, `tilesloaded` 이벤트에서 `map.relayout()` + `map.setCenter()`로 보정한다.
- **appkey는 카카오톡 공유용 `KAKAO_JS_KEY`와 별개 값** — 재현 청첩장(my-hompage) 프로젝트의 앱 키를 그대로 재사용한다. 이 앱은 이미 "카카오맵" 제품이 활성화되어 있고(2026-07 카카오 정책 변경 이후, 개발자 계정당 첫 번째로 활성화한 앱만 무료 쿼터를 받음 — 이 앱이 그 첫 앱), 이 프로젝트 자체 앱은 카카오맵 미활성화 상태라 별도 비용/활성화 없이 재사용을 택함. 대신 재현 앱 콘솔의 [앱 설정]→[플랫폼]→Web에 이 배포 도메인(`junil-hyeyoon-invite.vercel.app`)이 등록되어 있어야 하고, 그 프로젝트가 삭제/비활성화되면 이 지도 기능도 함께 끊기는 결합 위험이 있다.

**핵심 고려사항**
- 지도 조작(드래그/줌)은 `setupMapLock()`으로 초기엔 잠가둬서 스크롤을 방해하지 않다가, `.map-overlay`를 한 번 탭하면 잠금이 풀리며 오버레이가 `pointer-events:none`으로 사라져 지도를 직접 조작할 수 있게 한다. Google 폴백 경로는 기존 `setupIframeOverlay()`(터치 이동은 iframe에 전달, 클릭은 차단) 로직을 그대로 사용.
- 좌표 사용 여부는 [9. 예식장 정보 동적 관리](#9-예식장-정보-동적-관리)의 `useCoords` 플래그를 그대로 공유 — 이 플래그를 끄면 좌표가 있어도 지도 미리보기도 Google 주소검색으로 강제 전환된다.
- `index.html` / `groom-parent_2.html` / `bride-parent_3.html` 3개 파일에 동일하게 적용되어 있어, 지도 관련 수정 시 세 파일 모두 함께 바꿔야 한다([1. 다중 청첩장 변형](#1-다중-청첩장-변형-메인--신랑측--신부측)의 drift 위험 참고).

---

## 6. RSVP (참석 회신 + 집계)

**무엇인가**
청첩장 하단 폼에서 이름·연락처·신랑/신부측·참석 여부·인원수 입력 → `/api/rsvp` POST 로 KV 저장. `result.html` 에서 전체 목록 조회·개별 삭제·전체 초기화 + CSV 내보내기 가능. 회신 마감일 안내 문구는 **예식일 3주 전 날짜를 자동 계산**해서 표시 (더 이상 `setting.json`의 `rsvp.deadline` 수동 값을 사용하지 않음).

**왜 가치 있는가**
종이 청첩장 시대의 회신 엽서를 디지털화. 신랑신부와 웨딩홀이 실시간 인원 파악 가능. 마감일 자동 계산으로 예식 날짜가 바뀌어도 별도로 마감일을 다시 계산해서 입력할 필요가 없다.

**구현 난이도와 트레이드오프**
- **장점**: 폼 + 단일 KV 키만으로 MVP 완성, 비용 거의 0. 마감일 자동 계산으로 데이터 정합성 보장(날짜-마감일 불일치 방지)
- **단점**: 인증 없음 → `result.html` URL 노출 시 누구나 응답 목록 열람·삭제 가능 (단순 obscurity 의존). 응답 1건 추가마다 전체 배열 재저장 → 응답 수 천 건 이상이면 성능·동시성 문제 발생 가능

**핵심 고려사항**
- 필수 검증: `name`, `side`, `attend` — 누락 시 400 반환
- 마감일 계산: `renderRsvp()`에서 `CFG.date`(연/월/일) 기준 21일을 빼서 표시. [10. 예식 일자 동적 관리](#10-예식-일자-동적-관리)에서 날짜를 바꾸면 자동으로 함께 바뀜

---

## 7. 카카오톡 공유 (Kakao Share SDK)

**무엇인가**
공유 버튼 클릭 → Kakao Share SDK 의 `sendDefault` 호출로 feed 템플릿(타이틀·설명·이미지·버튼 2개) 카드를 카카오톡 채팅창에 보냄. 카드 본문 클릭은 청첩장 URL, 버튼은 청첩장 보기 + 길찾기(`nav.html`로 이동).

**왜 가치 있는가**
한국 결혼 청첩장 공유의 1순위 경로가 카카오톡. 일반 URL 만 전송하면 미리보기가 부실하지만 SDK 를 쓰면 풀 카드로 노출되어 클릭률이 높아짐.

**구현 난이도와 트레이드오프**
- **장점**: SDK 1줄 호출, 미리보기 이미지·버튼 커스텀 가능
- **단점**: Kakao 개발자 콘솔 등록 + 도메인 화이트리스트 필요, 카카오 정책 변경 시 미리보기 캐시 무효화 위해 이미지 URL 쿼리 버전(`?v=5`) 필요. **카카오톡 자체도 링크 미리보기를 서버에서 캐싱**하므로, 예식장/날짜를 바꾼 뒤 이미 공유했던 링크는 한동안 옛 정보가 그대로 뜰 수 있음 → 링크 뒤에 임의 쿼리(`?v=2`)를 붙여 새 링크로 취급되게 하면 캐시 우회 가능
- feed 템플릿은 버튼 최대 2개 제한 → 청첩장 보기/길찾기/일정 등록 중 선택 (현재는 청첩장 보기 + 길찾기, 일정 등록은 주석 보존)

**핵심 고려사항**
- 카드 설명 문구는 `CFG.venue.name`(navName 아님) + `CFG.venue.hall` + 날짜/시간 조합
- 4개 페이지(`index/groom_2/bride_3/link` 계열) 모두 동일 패턴으로 호출 → 변형마다 보낸 사람 이름이 다른 점만 유의
- 청첩장 본문(`index.html`/`groom-parent_2.html`/`bride-parent_3.html`) 하단, 푸터 바로 위에 카카오톡 공유 + 링크 복사 버튼이 있는 "청첩장 공유" 섹션이 있다. `shareKakao()`의 `siteUrl`과 `copyInviteLink()`의 `url` 모두 **`location.href`**를 사용 — 신랑측/신부측 변형 페이지 어디서 누르든, `?contact=` 쿼리 상태가 뭐든 지금 보고 있는 URL 그대로 공유·복사된다(과거엔 항상 사이트 루트(`/`)로 고정되어 있던 버그를 수정함)
- 카카오톡 공유는 **두 가지 서로 다른 값**을 쓴다는 점에 유의: URL을 그냥 붙여넣었을 때 뜨는 미리보기는 `<head>`의 정적 `og:*` 메타 태그(크롤러가 JS 없이 읽음)를 쓰고, 이 섹션의 "카카오톡 공유" 버튼은 `Kakao.Share.sendDefault()`로 그 자리에서 만든 JS 값을 쓴다. 하나를 고쳤다고 다른 하나가 자동으로 맞춰지지 않는다.

---

## 8. 혼주·계좌·연락처 통합 관리

**무엇인가**
photo.html에 있던 "계좌번호 관리"와 "혼주 연락처 관리" 두 카드를 **인물 1명당 계좌(예금주/은행/계좌번호) + 연락처(전화번호)를 한 블록에서 관리**하는 카드 하나로 통합. 신랑 본인/신부 본인/신랑 아버지/신랑 어머니/신부 아버지/신부 어머니 6명을 한 화면에서 관리.

**왜 가치 있는가**
같은 사람(예: 신랑 아버지)의 계좌 정보와 연락처 정보가 서로 다른 카드에 흩어져 있으면 입력 누락·불일치 위험이 크다. 한 블록으로 묶으면 관리 단위가 "사람" 기준으로 직관적이다.

**구현 난이도와 트레이드오프**
- **장점**: UI는 하나로 통합됐지만, **저장은 내부적으로 `/api/accounts`와 `/api/parents` 두 곳에 나눠서 PUT** — 기존에 이 두 엔드포인트를 따로 읽던 청첩장 페이지들의 코드는 전혀 건드릴 필요 없었음 (관리자 UI만 재구성한 사례)
- **단점**: 저장 버튼 하나가 API 호출 2번을 트리거하므로, 둘 중 하나만 실패하는 경우(네트워크 이슈 등)에 대한 부분 실패 처리는 현재 단순함(전체 실패로 안내)

**핵심 고려사항**
- 신랑/신부 본인은 `parents` API 상 `groomContact`/`brideContact`(전화만) 구조로 저장되고, 부모 4명은 `groomFather`/`groomMother`/`brideFather`/`brideMother`(성함+전화) 구조로 저장됨 — 계좌 쪽 "성함/예금주" 입력값과 연락처 쪽 "성함"이 하나의 입력창을 공유하도록 통합해 중복 입력 제거

---

## 9. 예식장 정보 동적 관리

**무엇인가**
예식장이 통째로 바뀌는 경우(예: 문수컨벤션 → 더엠컨벤션)까지 재배포 없이 photo.html에서 처리할 수 있도록 만든 기능 묶음.

- **기본 정보**: 예식장 이름(`name`), 홀 이름(`hall`), 길찾기 검색 이름(`navName`, 선택), 주소(`address`), 위도/경도(`lat`/`lng`), 좌표 우선 사용 여부(`useCoords`), 예식장 사진(`photo`, base64 업로드)
- **주소 → 좌표 자동 변환**: "🔍 주소로 좌표 찾기" 버튼 → `/api/geocode`(카카오 로컬 주소 검색 API 프록시) 호출 → 위도/경도 자동 입력 + 지도 미리보기(구글맵 임베드)로 바로 시각 확인
- **다중 장소(`extraLocations`)**: 지상/지하주차장처럼 예식장 본체 외 추가로 안내할 장소를 "+ 장소 추가" 버튼으로 원하는 만큼 등록. 각 장소마다 이름/주소/좌표/좌표우선여부를 독립적으로 관리하며, 청첩장 "오시는 길" 섹션에 `[이름] 주소 카드 + [이름] 길찾기 버튼`이 등록된 개수만큼 자동으로 반복 생성됨

**왜 가치 있는가**
결혼 준비 과정에서 예식장 변경은 드물지만 실제로 일어나는 일이고, 발생하면 청첩장의 커버·오시는 길·지도·길찾기·카카오 공유·일정 등록(cal.html)까지 연쇄적으로 갱신해야 한다. 이 모든 걸 코드 수정 없이 한 화면에서 처리할 수 있어야 실사용에 견딘다.

**구현 난이도와 트레이드오프**
- **장점**: `/api/venue` 하나의 KV 키에 예식장 관련 정보를 전부 모아 저장 → 청첩장 페이지들은 `init()`에서 이 한 번의 fetch로 `CFG.venue`를 덮어씀
- **단점**: 하나의 KV 키를 여러 관리자 카드(예식장 정보, 교통정보)가 공유하므로 저장 시 **읽기→병합→쓰기** 패턴이 필수 (그렇지 않으면 서로의 필드를 지움). 지오코딩은 카카오 개발자 콘솔에서 "카카오맵" 제품을 별도로 활성화해야 동작 — 활성화 안 하면 `NotAuthorizedError` 발생
- extraLocations가 늘어날수록 오시는 길 섹션이 길어짐 — 현재는 각 장소마다 지도 미리보기를 따로 그리지 않고 주소 카드+버튼만 반복(디자인 단순화)

**핵심 고려사항**
- `name`(화면 표시용) vs `navName`(길찾기 검색용 라벨)의 역할 구분은 [5. 길찾기](#5-길찾기-4종-지도-앱--좌표주소-우선순위) 참고
- 정적 메타 태그(og:description 등)와 nav.html의 초기 HTML(`<title>`, 화면 텍스트)은 이 KV 값을 자동 반영하지 않으므로 예식장이 바뀔 때 별도로 갱신 필요 — nav.html은 최소한 JS 로드 후에는 화면 텍스트도 함께 갱신되도록 처리되어 있음
- 웨딩홀 사진(`venue.photo`)은 "오시는 길" 섹션 **맨 위**(section-label/title보다도 앞)에 표시되도록 배치되어 있다 — 지도·주소보다 먼저 건물 사진으로 보여준 뒤 길찾기 정보로 이어지는 순서. 사진 미등록 시 `display:none`으로 완전히 숨겨져 레이아웃에 빈 공간이 남지 않음

---

## 10. 예식 일자 동적 관리

**무엇인가**
예식 연/월/일/시간을 photo.html의 날짜·시간 입력(`<input type="date">`/`<input type="time">`)으로 관리. 저장 시 **요일을 자동 계산**하고, "오후 12시 30분" 같은 한글 표시 문자열도 자동 생성해서 함께 저장한다.

**왜 가치 있는가**
예식장이 바뀌면 날짜/시간도 함께 조정되는 경우가 흔하다. 요일·표시 문자열을 수동 입력하게 하면 날짜와 요일이 어긋나는 실수가 생기기 쉬운데, 자동 계산하면 이 문제가 원천 차단된다.

**구현 난이도와 트레이드오프**
- **장점**: 날짜 하나만 바꾸면 커버의 D-day, RSVP 마감일(예식 3주 전), 캘린더 알림(예식 7일 전) 시각까지 연쇄적으로 자동 재계산됨
- **단점**: `cal.html`의 캘린더 알림 상세 문구(자유 서술형 텍스트)는 날짜/시간/예식장 이름까지 반영하도록 다시 조립하지만, 문장 자체의 표현(예: "~입니다.^^" 같은 어투)은 코드에 고정되어 있어 문구 톤을 바꾸려면 여전히 코드 수정이 필요

**핵심 고려사항**
- `cal.html`은 `/api/date` + `/api/venue`를 함께 fetch해서 Google Calendar "결혼식 7일전" 알림 이벤트의 UTC 시각(`startUTC`/`endUTC`)과 상세 설명을 실시간 재계산 (KST → UTC는 9시간 차 계산)
- `setting.json`의 `wedding.date`는 KV(`/api/date`)가 비어있을 때만 쓰이는 초기 기본값

---

## 11. 오시는 길 교통정보 관리

**무엇인가**
"오시는 길" 섹션의 Bus/Taxi/Parking/Car 4개 카드(2×2 그리드) 텍스트를 photo.html에서 자유 텍스트로 관리. 줄바꿈(`\n`)을 입력한 그대로 청첩장에 `<br>`로 반영.

**왜 가치 있는가**
버스 정류장 이름·번호, 택시 기사님께 말씀드릴 문구, 주차 안내, 공항/기차역 소요시간 등은 예식장마다 완전히 다른 실제 현장 정보라서 자동 계산이 불가능하다. 코드 수정 없이 텍스트만 바꿔 넣을 수 있어야 한다.

**구현 난이도와 트레이드오프**
- **장점**: `/api/venue`의 `transport` 필드 하나에 4개 텍스트를 모아 저장, 청첩장은 저장된 값이 있을 때만 기존 하드코딩 텍스트를 교체(없으면 안전하게 그대로 유지)
- **단점**: 자유 텍스트라 형식 검증이 없음 — 관리자가 직접 정확한 정보를 입력해야 함(자동 검증 불가)

**핵심 고려사항**
저장 시 예식장 정보 카드와 같은 `/api/venue` 키를 공유하므로 **읽기→병합→쓰기** 패턴 적용 (9번과 동일한 이유).

---

## 12. 신랑/신부 표시 이름 관리

**무엇인가**
표지 타이틀·카카오 공유 카드에 쓰이는 신랑/신부 이름을 photo.html의 "🤵 신랑 본인/👰 신부 본인" 예금주 입력칸(`/api/accounts`)에서 가져온다. 페이지 로드 시 `setting.json`의 `wedding.groom.name`/`wedding.bride.name`(풀네임 기본값)을 먼저 쓰고, `/api/accounts`에 저장된 값이 있으면 그걸로 덮어쓴다.

**왜 가치 있는가**
예금주 이름(풀네임, 예: 김준일)과 표지에 어울리는 애칭 스타일 이름(성 없이, 예: 준일)은 맥락에 따라 다르게 써야 한다. 이름 하나를 photo.html에서 관리하면서도, 자리에 따라 자동으로 알맞은 형태로 보여줄 수 있어야 재배포 없이 이름을 바꿀 수 있다.

**구현 난이도와 트레이드오프**
- **장점**: 이미 있던 예금주 입력칸을 그대로 재사용 — 새 admin UI를 추가하지 않고도 표지 이름까지 실시간으로 바뀜
- **단점**: 예금주 칸에 무엇을 입력하느냐(풀네임 vs 애칭)에 따라 표지 겉모습이 달라짐 — 관리자가 이 칸의 이중 역할(계좌 표시용 + 표지 이름 소스)을 인지하고 있어야 함

**핵심 고려사항**
- `shortName(n)` 헬퍼: 이름이 3글자 이상(성+이름)이면 맨 앞 성 1글자를 잘라 2글자 애칭만 반환, 2글자 이하면 그대로 반환. 표지 `<title>`/카카오 공유 카드 타이틀처럼 "성이 필요 없는" 자리에서 사용
- 예금주 이름(계좌·송금 모달 표시용)은 원본 그대로(풀네임) 사용 — `shortName()`을 거치지 않음
- `index.html`/`groom-parent_2.html`/`bride-parent_3.html`은 물론, `link.html`/`girl_link.html`/`man_link.html`(청첩장 공유 진입 페이지)의 카카오 공유 타이틀에도 동일하게 `/api/accounts` 연동 + `shortName()`이 적용되어 있음 — 총 6개 파일

---

## 13. 커버 표시 관리 (영상/사진 · 예식 일정 사진)

**무엇인가**
청첩장 맨 위 커버는 기본적으로 배경 영상(`top.mp4`)을 보여주지만, photo.html 최상단 "🖼 메인 사진 (커버)" 카드에서 "영상"/"사진" 중 선택하고 사진을 등록하면 그 사진으로 대체된다(`/api/cover`, `{mode, photo}`). 별도로 "예식 일정" 섹션 중간(시간 표시와 D-Day 사이)에도 photo.html "📅 예식 일자 관리" 카드 하단에서 등록한 사진이 있을 때만 표시된다(`/api/date`의 `photo` 필드).

**왜 가치 있는가**
정적 배경 영상만으로는 커플의 실제 사진을 못 보여준다는 한계가 있었다. 재배포 없이 관리자 화면에서 영상↔사진을 오갈 수 있어야, 웨딩 스튜디오 사진이 나온 뒤 바로 교체할 수 있다.

**구현 난이도와 트레이드오프**
- **장점**: `mode`가 `photo`이고 사진이 등록된 경우에만 전환되고, 그 외엔 항상 기존 영상으로 안전하게 폴백 — 미설정 상태에서도 커버가 비어 보이는 일이 없음
- **단점**: 커버 사진과 예식 일정 사진은 서로 다른 KV 키(`wedding_cover` / `wedding_date`)에 저장되는 별개 기능이라, 관리자가 "사진을 어디에 넣어야 하는지" 헷갈릴 수 있음 — photo.html에 각각 어디에 반영되는지 설명 문구를 붙여둠

**핵심 고려사항**
- 커버 사진 모드로 전환되면 기존 배경 영상은 `pause()` + `display:none`으로 완전히 멈춰서 불필요한 재생/배터리 소모를 막음
- 커버 사진은 영상과 달리 좌우반전(`transform: scaleX(-1)`)을 적용하지 않음(`.cover-photo` 클래스로 오버라이드) — 실제 인물 사진이 반전되면 부자연스럽기 때문
- `index.html`/`groom-parent_2.html`/`bride-parent_3.html` 3개 파일 모두 `coverPhotoUrl`/`coverMode` 전역 변수 + 동일한 전환 로직을 가짐(drift 위험, [1. 다중 청첩장 변형](#1-다중-청첩장-변형-메인--신랑측--신부측) 참고)

---

## 14. 겨울 테마 리디자인

**무엇인가**
예식이 겨울(2026.12.27)에 열리는 데 맞춰 전체 디자인을 골드·크림·로즈 톤에서 스틸블루·아이스블루·페리윙클 등 차가운 겨울 톤으로 전환. 커버에는 은은한(투명도 16%) 후지산 실루엣 SVG 워터마크를 배경에 깔고, 기존에 흩날리던 단풍잎 장식(`.petal`)을 순백~은청 그라데이션의 눈송이로 교체(아래로 흩날리며 떨어지는 모션). "마음 전하기" 카드의 판다/토끼 캐릭터도 턱시도를 입은 신랑·면사포를 쓴 신부 사람 모양 SVG 캐릭터로 새로 그렸다.

**왜 가치 있는가**
청첩장 첫인상은 커버·색감이 좌우한다. 예식 계절에 맞는 톤으로 통일하면 "겨울 눈꽃축제" 무드가 살아나고, 판다/토끼 같은 범용 캐릭터보다 신랑신부를 직접 형상화한 캐릭터가 청첩장다운 느낌을 준다.

**구현 난이도와 트레이드오프**
- **장점**: 색상은 전부 CSS 커스텀 프로퍼티(`--gold`, `--rose` 등, 변수명은 유지하고 값만 교체)와 하드코딩된 hex 값을 스크립트로 일괄 치환해서 3개 청첩장 파일 + `link.html`/`girl_link.html`/`man_link.html`/`nav.html`/`cal.html`까지 손님용 페이지 전체에 통일감 있게 적용
- **단점**: 카카오/네이버/토스/구글 캘린더 버튼처럼 **외부 브랜드 고유 색상은 치환 대상에서 의도적으로 제외**해야 해서, 전역 치환 스크립트를 짤 때 브랜드 색상 hex 목록을 미리 분리해둬야 함(안 그러면 카카오 버튼이 파랗게 바뀌는 사고 발생)

**핵심 고려사항**
- 후지산 SVG는 실사진이 아니라 `<path>`로 직접 그린 벡터 실루엣(`#cover-fuji`) — `.cover` 섹션의 배경 영상/사진보다 뒤(`z-index:0`)에 깔림
- 눈 효과는 `.petals-overlay`/`.petal` 클래스명을 그대로 유지한 채 내부 모양(원형)·색상(백색~은청)·애니메이션(아래로 낙하)만 교체 — `prefers-reduced-motion`에서는 기존과 동일하게 비활성화됨
- 관리자 페이지(`photo.html`, `result.html`)는 이번 리디자인 대상에서 제외 — 손님이 보지 않는 내부 도구라 기존 골드 톤 유지
- `og:image:alt` 텍스트도 "한복 입은 판다 신랑과 토끼 신부 일러스트"에서 실제 커버 사진 설명으로 갱신됨

---

## 15. 빠른 참조: 기능별 코드 진입점

| 기능 | 주요 파일 | 핵심 함수/식별자 |
|---|---|---|
| 갤러리 슬라이더 | `index.html` 외 2종 | `renderGallery()`, `goSlide()`, `setupSliderSwipe()`, `#sliderTrack` |
| 카카오 공유 | `index.html` 외 3종 (`link.html` 계열 포함) | `sendKakao()`, `Kakao.Share.sendDefault` |
| 송금 모달 | `index.html` 외 2종 | 모달 컨테이너 + 토스/카카오페이 딥링크 핸들러 |
| 길찾기 | `nav.html`(단독 페이지) + 청첩장 모달 | `goNav(app)`, `openNavModal(target)`, `openNavModalByIndex(i)`, `activeNavTarget` |
| 지도 미리보기 | `index.html` 외 2종 | `renderVenue()` 내 카카오맵/Google 분기, `setupMapLock()`, `setupIframeOverlay()`, `#kakao-map` |
| RSVP | `index.html`, `result.html`, `/api/rsvp` | 폼 submit 핸들러 + KV CRUD, `renderRsvp()`(마감일 자동계산) |
| 사진 업로드 | `photo.html`, `/api/photos` | `compressImage()` + POST |
| 예식장 정보/추가 장소/교통정보 | `photo.html`, `/api/venue` | `saveVenue()`, `saveTransport()`, `findCoordsFromAddress()`, `renderExtraLocations()` |
| 예식 일자 | `photo.html`, `/api/date` | `saveWeddingDate()`, `formatKoreanTime()` |
| 지오코딩 | `api/geocode.js` | 카카오 로컬 주소 검색 API 프록시 (서버에서 `KAKAO_REST_API_KEY` 사용) |
| 혼주·계좌·연락처 | `photo.html`, `/api/accounts`, `/api/parents` | `saveAccountsAndContacts()`, `PERSON_FIELDS` |
| 캘린더 알림 재계산 | `cal.html` | `loadDynamicOverrides()` (EVT.startUTC/endUTC/details/location) |
| 신랑/신부 표시 이름 | `index.html` 외 5종, `/api/accounts` | `shortName()`, init() 내 accounts fetch override |
| 커버 영상/사진 전환 | `index.html` 외 2종, `photo.html`, `/api/cover` | `coverPhotoUrl`, `coverMode`, `loadCover()`, `saveCover()` |
| 예식 일정 사진 | `index.html` 외 2종, `photo.html`, `/api/date`의 `photo` | `#date-photo-wrap`, `loadWeddingDate()`/`saveWeddingDate()` 내 `datePhotoData` |
| 업로드 용량 제한 | `photo.html` | `compressImage()` 내 `MAX_UPLOAD_BYTES`(2.5MB) 체크 |

---

## 16. 보완 권장 사항 (현 시점에서 식별된 갭)

1. **세 청첩장 파일 공통 JS 분리** — `js/gallery.js`, `js/kakao-share.js`, `js/nav.js` 형태로 외부화하면 drift 사라짐 (여전히 미해결 — 이번 겨울 테마·카카오맵 작업에서도 3개 파일 동일 패치를 반복함)
2. ~~**관리 페이지 인증**~~ — `photo.html`은 비밀번호 게이트 적용됨(완료). `result.html`은 여전히 별도 인증 없음(미해결)
3. **사진 외부 스토리지 마이그레이션** — base64 누적이 KV 비용에 부담되면 Vercel Blob 또는 S3 로 이전 (미해결). 업로드 용량 자체는 2.5MB로 제한해둠(완료, [3. 사진 갤러리](#3-사진-갤러리-업로드--슬라이더) 참고)
4. **정적 메타 태그 자동화** — 카카오톡 공유 미리보기(og:description 등)를 KV 값과 실시간 동기화하려면 Edge/Serverless 함수로 SSR 전환 필요. 논의 결과 구조 단순성을 위해 **의도적으로 보류** — 필요해지면 다시 다루기로 함 (미해결, 우선순위 낮음)
5. **카카오 공유 미리보기 이미지 캐시** — Kakao 캐시 무효화는 쿼리 버전(`?v=6`)으로 처리 중. 카카오톡 자체의 **페이지 URL 캐시**는 이미지 쿼리를 바꿔도 풀리지 않으므로, 실제로 옛 정보가 그대로 뜨면 [카카오 공유 디버거](https://developers.kakao.com/tool/debugger/sharing)에 URL을 넣어 강제 재크롤링해야 함
6. **딥링크 동작 자동 검증** — 송금/길찾기 앱 URL 스킴 변경 시 알 수 있도록 분기별 수동 체크리스트 또는 CI 의 가벼운 모니터링 (미해결)
7. **카카오맵/재현 프로젝트 결합 위험** — 지도 미리보기 appkey와 지오코딩 REST 키 모두 재현 청첩장(my-hompage) 프로젝트의 카카오 앱을 재사용 중이라, 그 프로젝트를 정리/삭제하면 이 두 기능이 함께 끊김 ([5-1](#5-1-지도-미리보기-카카오맵-js-sdk) 참고, 의도적으로 감수한 트레이드오프)
8. ~~**관리자 폼 기본값에 남은 실제 개인정보**~~ — photo.html의 신랑/신부/부모님 4명 계좌·연락처 입력칸 기본값에 이전 커플의 실명·실계좌번호가 남아있던 것을 전부 정리함(완료)
9. **og:image 선언 비율과 실제 사진 비율 불일치** — `og:image:width/height`는 1200×630(가로)으로 고정 선언되어 있지만 실제 커버 사진은 세로(720×864) — 카카오톡 등에서 미리보기가 다소 어색하게 보일 수 있음(현재는 실사용 확인 후 필요시 대응하기로 보류, 미해결)

---

## 17. 관련 문서

- [`README.md`](README.md) — 프로젝트 소개, `setting.json` 스키마, 실행 방법
- [`갤러리_슬라이더_구조.md`](갤러리_슬라이더_구조.md) — 갤러리 슬라이더 상세 구조, 진단 로그 카탈로그, 수정 워크플로우
